version: '3'

services:
  discord-bot:
    build: .
    restart: unless-stopped
    volumes:
      - ./logs:/app/logs
      - ./setting.json:/app/setting.json
      - ./assets:/app/assets
    env_file:
      - .env
    environment:
      - TZ=Asia/Taipei 